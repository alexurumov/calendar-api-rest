export enum Period {
    TODAY = 'today',
    PAST = 'past',
    FUTURE = 'future'
}

export enum Repeated {
    NO = 'no',
    DAILY = 'daily',
    WEEKLY = 'weekly',
    MONTHLY = 'monthly'
}

export enum Answered {
    YES = 'yes',
    NO = 'no',
    PENDING = 'pending'
}
